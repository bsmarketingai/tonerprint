Stránkování výpisu.

```jsx
<Pagination page={3} pages={42} onChange={setPage} />
```

Krajní strany zůstávají vždy vidět, prostředek se sbaluje výpustkou. Doplňuje se tlačítkem "Načíst dalších 24" nad ním — stránkování samo je záložní cesta pro klávesnici a vyhledávače.
